# Story 2.3: Define Shared Contracts And Alert Publisher

Status: ready for review

## Story

As the API service,
I want to publish a complete climate-alert message to RabbitMQ when a measurement triggers an alert,
so that the Notification service can process notifications without internal module calls or cross-service database joins.

## Acceptance Criteria

1. A shared `ClimateAlertDetectedMessage` contract exists in `libs/contracts`.
2. The contract contains all data required by the Notification service to notify recipients.
3. API service defines an alert publisher port used by `RecordMeasurementService`.
4. RabbitMQ publisher adapter implements the alert publisher port.
5. The existing in-process `EventEmitter2` notification flow is removed from the alert delivery path.
6. Measurement saving remains successful even if notification dispatch is asynchronous.
7. Unit tests verify publishing happens only when a measurement has an alert.
8. Publisher failures are handled according to an explicit policy and documented.

## Tasks / Subtasks

- [x] Task 1: Define the integration contract (AC: #1, #2)
  - [x] 1.1 Create `ClimateAlertDetectedMessage` in `libs/contracts`.
  - [x] 1.2 Include `messageId`, `occurredAt`, `measurementId`, `stationId`, `stationName`, `alertType`, `reportedAt`, `temperature`, `humidity`, and `pressure`.
  - [x] 1.3 Avoid exporting domain entities or value objects in the contract.

- [x] Task 2: Add an alert publisher port in the API service (AC: #3)
  - [x] 2.1 Define `AlertPublisher` as a port interface.
  - [x] 2.2 Add an injection token for the publisher.
  - [x] 2.3 Inject the publisher into `RecordMeasurementService`.

- [x] Task 3: Publish from measurement recording (AC: #5, #6)
  - [x] 3.1 Build the message after the measurement is saved.
  - [x] 3.2 Publish only when `measurement.hasAlert()` is true.
  - [x] 3.3 Remove direct dependency on the Notification module and EventEmitter notification handler.

- [x] Task 4: Implement RabbitMQ adapter (AC: #4, #8)
  - [x] 4.1 Add RabbitMQ client dependency and connection configuration.
  - [x] 4.2 Publish to a configured exchange/routing key.
  - [x] 4.3 Document whether publisher errors fail the request or are logged after persistence.

- [x] Task 5: Add tests (AC: #7)
  - [x] 5.1 Update `RecordMeasurementService` tests to assert alert messages are published.
  - [x] 5.2 Add a no-alert test that asserts no message is published.
  - [x] 5.3 Add publisher adapter unit tests with a mocked RabbitMQ channel/client.

## Dev Notes

- Recommended failure policy: measurement persistence succeeds first; publishing errors should be surfaced or logged explicitly. If no outbox is implemented, document the residual risk.
- A future outbox pattern would be more robust, but it may be too much for this academic delivery unless time allows.
- The message should be rich enough that the Notification service does not need to read the API service database for measurement/station details.

## Implementation Notes

- `ClimateAlertDetectedMessage` is a primitive, JSON-serializable integration contract. Dates are ISO strings so the Notification service can consume messages from RabbitMQ without relying on API process memory or domain classes.
- `RecordMeasurementService` saves the measurement before publishing. It publishes only when `measurement.hasAlert()` is true.
- Publisher failure policy: RabbitMQ publisher errors are logged after persistence and do not fail or roll back the measurement request. Without an outbox, there is a residual risk that a saved alert measurement may not be delivered if RabbitMQ is unavailable at publish time.
- The API alert delivery path no longer emits `MeasurementAlertDetectedEvent` through `EventEmitter2`; it uses the `AlertPublisher` port and the RabbitMQ adapter.

## References

- [Source: src/modules/measurements/application/services/record-measurement.service.ts - current EventEmitter flow]
- [Source: docs/architecture/sequences/record-measurement-alert-sequence.mmd - current in-process alert sequence]
- [Source: docs/informe-desarrollo-ddd.md - separation between alert detection and notification sending]
