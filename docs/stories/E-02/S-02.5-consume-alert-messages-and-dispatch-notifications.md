# Story 2.5: Consume Alert Messages And Dispatch Notifications

Status: draft

## Story

As the Notification service,
I want to consume climate-alert messages from RabbitMQ and notify matching subscribers,
so that climate alerts are handled asynchronously through a real service boundary.

## Acceptance Criteria

1. Notification service connects to RabbitMQ and consumes `ClimateAlertDetectedMessage` messages.
2. Consumer validates incoming message shape before processing.
3. Notification service resolves subscribers by `stationId` and `alertType`.
4. Users without configured delivery targets are skipped.
5. A fake/log notifier is implemented for local demos and tests.
6. Telegram adapter remains optional and is not required for Delivery II success.
7. Unit tests cover consumer behavior, preference filtering, missing targets, malformed messages, and notifier calls.
8. Failed message handling is explicit: ack, nack, retry, or dead-letter behavior is documented.

## Tasks / Subtasks

- [ ] Task 1: Add RabbitMQ consumer adapter (AC: #1, #8)
  - [ ] 1.1 Connect to configured RabbitMQ URL.
  - [ ] 1.2 Bind the queue to the climate alert exchange/routing key.
  - [ ] 1.3 Implement explicit ack/nack behavior.

- [ ] Task 2: Validate consumed messages (AC: #2)
  - [ ] 2.1 Add runtime validation for the shared message contract.
  - [ ] 2.2 Reject or dead-letter malformed messages according to the selected policy.

- [ ] Task 3: Implement alert handling use case (AC: #3, #4)
  - [ ] 3.1 Query notification preferences by station id.
  - [ ] 3.2 Filter preferences by alert type.
  - [ ] 3.3 Resolve configured delivery targets.
  - [ ] 3.4 Skip preferences without delivery targets.

- [ ] Task 4: Add notifier adapters (AC: #5, #6)
  - [ ] 4.1 Implement `LogAlertNotifierAdapter` or `FakeAlertNotifierAdapter`.
  - [ ] 4.2 Keep existing Telegram adapter optional behind configuration.
  - [ ] 4.3 Ensure no real external network call is required for local demo.

- [ ] Task 5: Add unit tests (AC: #7)
  - [ ] 5.1 Test consumer delegates valid messages to the application use case.
  - [ ] 5.2 Test malformed messages are handled by the configured failure policy.
  - [ ] 5.3 Test subscriber filtering by `stationId` and `alertType`.
  - [ ] 5.4 Test users without delivery targets are skipped.
  - [ ] 5.5 Test fake/log notifier receives expected payload.

## Dev Notes

- This story is where the project satisfies the mandatory queue publication/consumption requirement.
- Keep the consumed message as an integration DTO, then map it into Notification service application models.
- Avoid importing API application services, API repositories, or API domain entities.

## References

- [Source: src/modules/notifications/application/services/notification.service.ts - current recipient filtering logic]
- [Source: src/modules/notifications/infrastructure/adapters/telegram-alert-notifier.adapter.ts - current delivery adapter]
- [Source: docs/informe-desarrollo-ddd.md - notification delivery as support behavior]

