# Story 2.6: Complete Station And Measurement Search Filters

Status: done

## Story

As an API client,
I want to search stations and measurements by the required criteria,
so that Delivery II exposes station/measurement search, date range filtering, and climate-parameter filtering through the API.

## Acceptance Criteria

1. Stations can be searched by station name through the API.
2. Measurements can be searched by station name.
3. Measurements can be filtered by date range.
4. Measurements can be filtered by temperature range, greater-than, and less-than semantics.
5. Measurements can be filtered by humidity range, greater-than, and less-than semantics.
6. Measurements can be filtered by pressure range, greater-than, and less-than semantics.
7. Invalid ranges return a clear `400` response.
8. Repository queries are covered by unit tests or integration-style repository tests with mocked models.
9. API docs and DTOs document all query parameters.

## Tasks / Subtasks

- [x] Task 1: Add station-name search endpoint or query param (AC: #1)
  - [x] 1.1 Add a `name` query parameter to station listing, or add a dedicated search endpoint.
  - [x] 1.2 Implement case-insensitive partial matching.
  - [x] 1.3 Add controller/service/repository tests.

- [x] Task 2: Extend measurement query DTO (AC: #2, #3, #4, #5, #6, #9)
  - [x] 2.1 Add `reportedFrom` and `reportedTo`.
  - [x] 2.2 Add humidity filters: `humidityMin`, `humidityMax`.
  - [x] 2.3 Add pressure filters: `pressureMin`, `pressureMax`.
  - [x] 2.4 Keep or document temperature filters: `tempMin`, `tempMax`.
  - [x] 2.5 Add Swagger metadata and validation decorators.

- [x] Task 3: Validate all ranges (AC: #7)
  - [x] 3.1 Reject `tempMin > tempMax`.
  - [x] 3.2 Reject `humidityMin > humidityMax`.
  - [x] 3.3 Reject `pressureMin > pressureMax`.
  - [x] 3.4 Reject `reportedFrom > reportedTo`.

- [x] Task 4: Update repository filtering (AC: #2, #3, #4, #5, #6)
  - [x] 4.1 Add date-range query on `reportedAt`.
  - [x] 4.2 Add humidity query conditions.
  - [x] 4.3 Add pressure query conditions.
  - [x] 4.4 Preserve station-name lookup behavior.

- [x] Task 5: Add tests (AC: #8)
  - [x] 5.1 Test query service validation for every invalid range.
  - [x] 5.2 Test repository builds expected Mongo filters.
  - [x] 5.3 Test controller maps DTO values into the service command.

## Dev Notes

- Current implementation only supports station name, temperature min/max, and alert-only filters.
- The requirement says ranges, greater than, and less than. Min-only and max-only parameters satisfy greater-than/less-than semantics if documented clearly.
- Date range should use ISO-8601 query values and map to `Date` only after DTO validation.

## References

- [Source: src/modules/measurements/interface/dtos/measurement.dto.ts - current query DTO]
- [Source: src/modules/measurements/application/services/query-measurements.service.ts - current range validation]
- [Source: src/modules/measurements/infrastructure/repositories/mongo-measurement.repository.ts - current Mongo filtering]
- [Source: docs/api-reference.md - current API filter documentation]
