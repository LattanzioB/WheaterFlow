# Historia 3.15: Documentación de arquitectura de la Entrega III

Estado: completada

## Historia

Como revisor, quiero documentación sincronizada con la solución final, para
comprender la evolución arquitectónica y reproducir la demostración.

## Criterios de aceptación

1. C4 muestra API, notificaciones, ingesta, OWM y observabilidad.
2. Se justifican los desintegradores que motivan el componente independiente.
3. Hay secuencias para:
   - ingesta periódica (cron → OWM → API → persistencia),
   - alerta por medición OWM (API → RabbitMQ → notificaciones),
   - **temperatura actual** (cliente → API → ingesta → OWM → respuesta),
   - **promedios diario/semanal** (cliente → API → MongoDB → respuesta).
4. Swagger documenta proveedor, origen, endpoint interno y reportes.
5. Se documentan las estrategias de resiliencia y sus fronteras (OWM, API ↔
   ingesta).
6. Se documentan logs, métricas, tracing, reglas de alertas y dashboard.
7. Se adjuntan resultados de carga y evidencia de al menos una semana de datos.
8. README explica arranque, configuración, URLs y recorrido de demo.
9. Diagramas, enlaces y referencias pasan las validaciones del repositorio.

## Tareas

- [x] Actualizar C4, modelo de dominio y diagramas de secuencia.
- [x] Crear `current-temperature-report-sequence.mmd` (API → ingesta → OWM).
- [x] Crear secuencia de promedios (API → MongoDB).
- [x] Actualizar referencia API y Swagger.
- [x] Consolidar resiliencia, observabilidad y resultados de carga.
- [x] Registrar evidencia operativa y guion de demostración.

## Implementación

- C4 actualizado en `docs/architecture/c4/` para incluir OpenWeather,
  Ingestion service, reportes y observabilidad.
- Modelo de dominio actualizado con `WeatherProviderCode` y
  `MeasurementSource`.
- Puertos/adaptadores actualizados con los puertos de ingesta, el cliente
  API → ingesta y el agregado de promedios.
- Secuencias agregadas:
  - `docs/architecture/sequences/current-temperature-report-sequence.mmd`
  - `docs/architecture/sequences/temperature-average-report-sequence.mmd`
- `docs/architecture-overview.md`, `docs/hexagonal-architecture.md`,
  `docs/api-reference.md`, `docs/setup.md`, `docs/load-tests/README.md` y
  `README.md` enlazan la arquitectura final y los recorridos de demo.
- `docs/delivery-iii-demo.md` consolida justificación de desintegradores,
  evidencia de carga, semana de datos, observabilidad y guion de demostración.

## Pruebas

- `npm test -- --runInBand --testPathIgnorePatterns="libs/shared/src/documentation"`
- `npm run build`
- `docker compose config --quiet`
- `graphify update .`

## Referencias

- [Fuente: docs/stories/E-03/S-03.9 - reporte temperatura actual vía ingesta]
- [Fuente: docs/stories/E-03/S-03.10 - promedios desde MongoDB]
- [Fuente: docs/stories/E-02/S-02.8 - patrón documentación Entrega II]
